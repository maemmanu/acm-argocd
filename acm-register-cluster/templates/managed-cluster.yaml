apiVersion: cluster.open-cluster-management.io/v1
kind: ManagedCluster
metadata:
  name: {{ .Values.clusterName }}
  labels:
    cloud: auto-detect
    vendor: auto-detect
    environment: {{ .Values.labels.environment | default "dev" }}
    operational-status: {{ .Values.labels.operational-status | default "alpha" }}
    region: {{ .Values.labels.region | default "not-defined" }}
    netpols: {{ .Values.labels.netpols | default "false" }}
spec:
  hubAcceptsClient: true
