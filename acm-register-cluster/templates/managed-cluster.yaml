apiVersion: cluster.open-cluster-management.io/v1
kind: ManagedCluster
metadata:
  name: {{ .Values.clusterName }}
  labels:
    environment: {{ .Values.env  }}
    operational: {{ .Values.operational  }}
    region: {{ .Values.region }}
    netpol: {{ .Values.netpol }}
spec:
  hubAcceptsClient: true
