apiVersion: cluster.open-cluster-management.io/v1
kind: ManagedCluster
metadata:
  name: {{ .Values.clusterName }}
  labels:
    cloud: auto-detect
    vendor: auto-detect
    environment: {{ .Values.env }}
    operational: {{ .Values.labe.operational | default "alpha" }}
spec:
  hubAcceptsClient: true
